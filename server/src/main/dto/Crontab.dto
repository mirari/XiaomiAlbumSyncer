export com.coooolfan.xiaomialbumsyncer.model.Crontab

input CrontabCreateInput {

    name

    description

    enabled

    syncMode

    config

    id(account) as accountId

    id(albums) as albumIds
}

input CrontabUpdateInput {

    name?

    description?

    enabled?

    syncMode?

    config?

    id(albums)? as albumIds
}